org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# The workstation folder has a Chinese product name. AGP's Windows guard is
# conservative; UTF-8/JDK 21 and the installed SDK toolchain handle this path.
android.overridePathCheck=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's Windows incremental cache cannot relativize plugin sources on C:
# against the temporary ASCII drive used to build this Chinese-named project.
kotlin.incremental=false
